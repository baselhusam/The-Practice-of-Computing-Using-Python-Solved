#Exercise 5

A_int = input('Enter an integer greater than 10: ')
while A_int > 10:
    A_int = A_int - 1
    print(A_int)
    

#Ans: Because the data type for the (A_int) variable is a String, and we can't compare (> operand) a string to an integer